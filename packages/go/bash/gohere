#! /usr/bin/env bash

gohere() {
    export GOPATH=$(pwd -P)
    if [ -d $GOPATH/_vendor ]; then
        GOPATH=$GOPATH/_vendor:$GOPATH
    fi
    if [ -d $GOPATH/Godeps ]; then
        GOPATH=$GOPATH/Godeps/_workspace:$GOPATH
    fi
}

goback() {
    export GOPATH=$HOME/go
}
